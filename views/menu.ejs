<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Menu</title>
</head>
<body>
   <nav> <%- include('./partials/nav') %> </nav>
  <h1>Full Menu</h1>
  <ul>
    <% menu.forEach((item) => { %>
    <li><strong> <%= item.rating === 5 ? ' ⭐' : '' %> <%= item.name %> </strong>  <%= item.details %> <i> <br><%= item.rating %>/5 stars <br> $<%= item.price %> </i> </li>
    <% }) %>
  </ul>
</body>
</html>
